name: Compile blueprint

on:
  push:
    branches:
      - main # Trigger on pushes to the default branch
      - master
  workflow_dispatch:        # Allow manual triggering of the workflow from the GitHub Actions interface

# Cancel previous runs if a new commit is pushed to the same PR or branch
concurrency:
  group: ${{ github.ref }}  # Group runs by the ref (branch or PR)
  cancel-in-progress: true  # Cancel any ongoing runs in the same group

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write         # Push the provenance ledger to its own branch (the referee action does)
  pages: write            # Write access to GitHub Pages
  id-token: write         # Write access to ID tokens
  issues: write           # Write access to issues
  pull-requests: write    # Write access to pull requests
  actions: read           # Read the previous run's artifact, for the referee revision diff

jobs:
  build_project:
    runs-on: ubuntu-latest
    name: Build project
    steps:
      - name: Cleanup to free disk space
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.0
        with:
          tool-cache: false
          android: true
          dotnet: false
          haskell: false
          large-packages: false
          docker-images: false
          swap-storage: false

      - name: Checkout project
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Load-bearing for provenance, not just a convenience: the ledger's edit half is
          # `git blame`, and at the default `fetch-depth: 1` blame attributes the entire library to
          # the single fetched commit.  The action would unshallow the checkout itself, but
          # fetching it here saves that second round trip.
          fetch-depth: 0 # Fetch all history for all branches and tags

      - name: Build and lint project
        uses: leanprover/lean-action@38fbc41a8c28c4cbaec22d7f7de508ec2e7c0dd9 # 2025-07-02
        with:
          build: true
          lint: true
          # Restricted to the Essakine2026Tight library below: the bare `mk_all --check` chokes on the
          # Comparator library, whose modules are globs with no module directory.
          mk_all-check: false

      - name: mk_all check (Essakine2026Tight library only)
        run: |
          set -euo pipefail
          lake exe mk_all --check --lib Essakine2026Tight

      # The comparator challenge/solution modules (see comparator/README.md). Building them here
      # keeps them compiling against the current tree, and `checkdecls` (inside the blueprint
      # step below) imports every library root of the workspace, so the Comparator library's stub
      # root module must have an olean by then. The library's real modules are globs, not roots,
      # exactly so that root-importing tools (checkdecls, referee) never load the challenges,
      # which redeclare project and LML names.
      - name: Build comparator challenges
        run: |
          set -euo pipefail
          lake build Comparator

      # The whole Referee pipeline: the binary and its toolchain check, semantic_hash, collect,
      # the provenance ledger's branch round trip, the revision-diff baseline, extract and
      # build-site. All of it used to be spelled out here, in 229 lines that were copy-pasted into
      # three repositories and then drifted apart. It now lives in the action, which is tested in
      # its own repository.
      #
      # Pinned to a tag rather than left floating: the tag names the Lean toolchain the binary was
      # built for (`v4.34.0-rc2` here, matching this project's lean-toolchain), and the action takes
      # its binary from the release of the same tag by default.
      - name: Build the referee site
        id: referee
        if: github.event_name == 'push'
        uses: LeanMachineLearning/exposition@v4.34.0-rc2-7
        with:
          root: Essakine2026Tight
          site-url: https://remydegenne.github.io/entropy-mdp/referee
          # An editorial claim, not a derived fact: it says whoever publishes this site vouches for
          # Mathlib and everything under it. Drop it and every upstream package counts as unaudited.
          trust: mathlib

      - name: Compile blueprint and documentation
        uses: leanprover-community/docgen-action@56023ee2a4b3630c1071825be5bafdb43f540bbd # 2026-08-21
        with:
          blueprint: true
          api-docs: false      # blueprint only: no doc-gen API documentation
          homepage: home_page
          build-page: false
          deploy: false

      # After the blueprint step, which writes `blueprint/` and `docs/` into the same folder. It
      # only ever adds, so the order is not load-bearing — but copying last keeps the referee output
      # out of reach of anything else that writes there.
      - name: Add the referee site to the home page
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          mkdir -p home_page/referee
          cp -r "${{ steps.referee.outputs.site }}/." home_page/referee/

      - name: "Upload website (API documentation, blueprint and any home page)"
        if: github.event_name == 'push'
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: home_page/

      - name: Deploy to GitHub Pages
        if: github.event_name == 'push'
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
